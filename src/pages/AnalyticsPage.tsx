import { Link } from 'react-router-dom';
import AppHeader from '../components/AppHeader';
import { usePatientSession } from '../features/patient/state/PatientSessionContext';
export default function AnalyticsPage(){const s=usePatientSession();const fields=Object.keys(s.historyAnswers??{}).length;const bg=Object.values(s.backgroundHistory).filter(Boolean).length;const reviewed=s.doctorReviews.filter(r=>r.status==='confirmed'||r.status==='edited').length;const items=s.documents.reduce((n,d)=>n+d.entities.length,0);return <div className="min-h-screen bg-canvas"><AppHeader/><main className="mx-auto max-w-5xl px-6 py-8"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-wide text-clinic-500">Sehat Saathi analytics</p><h1 className="mt-2 font-display text-3xl font-semibold">Prototype evaluation</h1><p className="mt-2 text-muted">Session-derived metrics only. No invented performance claims.</p></div><Link to="/doctor" className="rounded-full border border-clinic-200 px-5 py-2.5 text-sm">Back to physician view</Link></div><div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Structured fields",fields],["Background fields",bg],["Reviewed fields",reviewed],["Document clinical items",items]].map(([label,value])=><div key={String(label)} className="rounded-2xl border border-clinic-100 bg-white p-6 shadow-sm"><p className="text-sm text-muted">{label}</p><p className="mt-2 text-3xl font-semibold text-ink">{value}</p></div>)}</div><div className="mt-6 grid gap-5 lg:grid-cols-2"><div className="rounded-2xl border border-clinic-100 bg-white p-6 shadow-sm"><h2 className="font-display text-xl font-semibold">Safety flags</h2><p className="mt-2 text-4xl font-semibold text-ink">{s.safetyFlags.length}</p><p className="text-sm text-muted">configured flag(s) for this session</p></div><div className="rounded-2xl border border-clinic-100 bg-white p-6 shadow-sm"><h2 className="font-display text-xl font-semibold">Documents</h2><p className="mt-2 text-4xl font-semibold text-ink">{s.documents.length}</p><p className="text-sm text-muted">processed/uploaded record(s)</p></div></div><div className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-900">For real evaluation, use a fixed synthetic test set and calculate history completeness, normalization accuracy, OCR extraction accuracy, safety-rule recall and physician correction rate against a baseline.</div></main></div>}
