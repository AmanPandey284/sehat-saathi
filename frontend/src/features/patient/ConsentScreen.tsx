import { useNavigate } from 'react-router-dom';
import AppHeader from '../../components/AppHeader';
import { useLanguage } from '../../i18n/LanguageContext';
import { usePatientSession } from './state/PatientSessionContext';
export default function ConsentScreen(){const {t}=useLanguage();const nav=useNavigate();const {setConsentGranted}=usePatientSession();const agree=()=>{setConsentGranted(true);nav('/patient/profile');};return <div className="min-h-screen bg-canvas"><AppHeader/><main className="mx-auto flex max-w-2xl flex-col px-6 pb-20 pt-8"><div className="rounded-2xl border border-clinic-100 bg-white p-8 shadow-sm sm:p-10"><h1 className="font-display text-3xl font-semibold text-ink">{t.consent.title}</h1><p className="mt-3 text-muted">{t.consent.intro}</p><ul className="mt-6 space-y-4">{t.consent.points.map((point)=><li key={point} className="flex gap-3 text-ink"><span className="mt-1 h-2 w-2 flex-shrink-0 rounded-full bg-clinic-500"/>{point}</li>)}</ul><div className="mt-10 flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><button onClick={()=>nav('/')} className="rounded-full border border-clinic-200 px-7 py-3 text-lg text-muted">{t.consent.cancel}</button><button onClick={agree} className="rounded-full bg-clinic-600 px-7 py-3 text-lg font-medium text-white">{t.consent.agree}</button></div></div></main></div>}
